import { labelClasses } from "@mioto/design-system/Label";

export const labelContainerClasses = labelClasses({
  className:
    "border border-gray5 rounded bg-gray1 py-3 px-4 focus-within:inner-focus cursor-pointer text-mediumText",
});
